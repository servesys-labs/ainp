# Production Environment Variables Template
# Copy this file to .env.production and fill in real values
# DO NOT commit .env.production to git (it's in .gitignore)

# ============================================================================
# Railway Auto-Provided Variables
# ============================================================================
# These are automatically injected by Railway plugins/services
# You don't need to set these manually

# PostgreSQL (Railway plugin)
# DATABASE_URL=postgresql://postgres:password@host:port/database

# Redis (Railway plugin)
# REDIS_URL=redis://default:password@host:port

# NATS JetStream (Railway service - internal URL)
# NATS_URL=nats://nats.railway.internal:4222

# Railway runtime
# PORT=8080  # Auto-assigned by Railway

# ============================================================================
# User-Provided Variables (REQUIRED)
# ============================================================================
# You MUST set these via Railway dashboard: Settings → Variables

# OpenAI API Key (for capability embeddings)
OPENAI_API_KEY=sk-proj-...your-key-here...

# ============================================================================
# Application Configuration (Optional Overrides)
# ============================================================================

# Node.js environment
NODE_ENV=production

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# Rate limiting (requests per window)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

# Discovery cache TTL (seconds)
DISCOVERY_CACHE_TTL=300

# NATS JetStream configuration
NATS_STREAM_NAME=ainp-intents
NATS_STREAM_RETENTION_DAYS=7

# ============================================================================
# Security Configuration (Optional)
# ============================================================================

# CORS allowed origins (comma-separated)
CORS_ORIGINS=https://yourdomain.com

# JWT signing key (if using JWT auth)
# JWT_SECRET=your-secret-key-here

# ============================================================================
# Monitoring & Observability (Optional)
# ============================================================================

# Sentry DSN (error tracking)
# SENTRY_DSN=https://...@sentry.io/...

# Datadog API key (metrics)
# DATADOG_API_KEY=...

# ============================================================================
# Notes
# ============================================================================
# 1. Railway automatically provides DATABASE_URL, REDIS_URL, and PORT
# 2. NATS_URL must point to internal Railway service (nats.railway.internal:4222)
# 3. OPENAI_API_KEY is the ONLY required user-provided variable
# 4. Use Railway's built-in secret management - DO NOT commit real values
# 5. Verify health check: curl https://your-app.up.railway.app/health/ready
